# Pydantic-AI Validator

**Objective:** Define unit tests using TestModel:
- Text-only prompt → parametric family with correct parameters.
- Image-only input → mesh import with basic scale parameter.
- Combined input → merged result; parameters reflect text, shape reflects image.
